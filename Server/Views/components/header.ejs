<!-- Main Navigation -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/home">WEBD6201</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a id="home" class="nav-link <%= (page == 'home') ? 'active' : ''; %>" aria-current="page" href="/home"><i class="fas fa-home fa-lg"></i> Home</a>
          </li>

          <li class="nav-item">
            <a id="about"  class="nav-link <%= (page == 'about') ? 'active' : ''; %>" aria-current="page" href="/about"><i class="fas fa-info fa-lg"></i> About Us</a>
          </li>

          <li class="nav-item">
            <a id="projects"  class="nav-link <%= (page == 'projects') ? 'active' : ''; %>" aria-current="page" href="/projects"><i class="fas fa-cog fa-lg"></i> Projects</a>
          </li>

          <li class="nav-item">
            <a id="services"  class="nav-link <%= (page == 'services') ? 'active' : ''; %>" aria-current="page" href="/services"><i class="fas fa-map fa-lg"></i> Services</a>
          </li>

          <li class="nav-item">
            <a id="contact"  class="nav-link <%= (page == 'contact') ? 'active' : ''; %>" aria-current="page" href="/contact"><i class="fas fa-phone fa-lg"></i> Contact Us</a>
          </li>

          <% if(displayName) { %>
          <li id="contactListLink" class="nav-item">
          <a id="contact-list" class="nav-link <%= (page == 'contact-list') ? 'active' : ''; %>" aria-current="page" href="/contact-list"><i class="fas fa-users fa-lg"></i> Contact List</a>
        </li>

          <li id="loginListItem" class="nav-item">
            <a id="logout" class="nav-link" aria-current="page" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
          </li>
          <% } else { %>
            <li id="loginListItem" class="nav-item">
            <a id="login" class="nav-link <%= (page == 'login') ? 'active' : ''; %>" aria-current="page" href="/login"><i class="fas fa-sign-in-alt"></i> Login</a>
          </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>